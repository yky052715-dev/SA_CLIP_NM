# Install CUDA-enabled PyTorch first:
# python -m pip install torch==2.1.2 torchvision==0.16.2 \
#   --index-url https://download.pytorch.org/whl/cu121
#
# Then install this file. PyTorch is intentionally not listed below to avoid
# replacing the CUDA wheel with a CPU or incompatible build.
transformers>=4.41,<4.47
numpy>=1.24,<2.0
Pillow>=9.5
PyYAML>=6.0
scikit-learn>=1.3
scipy>=1.10
matplotlib>=3.7
pytest>=7.4
